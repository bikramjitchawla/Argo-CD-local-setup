apiVersion: skaffold/v4beta11
kind: Config
metadata:
  name: argocd

manifests:
  rawYaml:
    - install.yaml
    - selfsigned-clusterissuer.yaml
    - ingress-route.yaml
    - polaris-exempt-namespaces.yaml
    - applicationset.yaml

deploy:
  kubectl:
    defaultNamespace: argocd
